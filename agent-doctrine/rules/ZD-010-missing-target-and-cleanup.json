{
  "id": "ZD-010",
  "title": "Fail closed and clean exact owned state",
  "invariant": "A missing or ambiguous target is reported without invention, and teardown removes only exact test/task-owned objects.",
  "sharedInstruction": "When a target is missing or ambiguous, report the exact failure and stop rather than creating a substitute; clean only exact state owned by the task and prove the final roster.",
  "appliesTo": ["claude", "pi"],
  "projection": {
    "claude": { "mechanism": "Resolver failures plus explicit kill/session cleanup", "enforcement": "instruction", "promptGuideline": null },
    "pi": { "mechanism": "Typed target resolution plus tab_kill/session_kill", "enforcement": "typed-operation", "promptGuideline": null }
  },
  "caveats": { "claude": [], "pi": [] },
  "verifyRefs": ["skills/zmux/references/run-observe.md", "pi-zmux/references/testing/host-flow.md"]
}
