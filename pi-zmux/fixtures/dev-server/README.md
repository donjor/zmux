# Dev server fixture

Minimal dependency-free fixture for canonical `pi-zmux` runtime prompt cards.

```bash
npm run dev
```

The script binds an ephemeral localhost port, prints `ready localhost:<port>`, and stays alive until interrupted. `logs/app.txt` is a tracked harmless file for pane-tail cards.
