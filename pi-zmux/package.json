{
  "name": "pi-zmux",
  "version": "0.4.0",
  "description": "Canonical Pi dispatcher for deterministic zmux runtime and interactive terminal orchestration.",
  "private": true,
  "type": "module",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "test": "node test/run.mjs && node test/dispatcher.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "typebox": "^1.1.24"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.80.2",
    "@types/node": "^24.10.1",
    "typescript": "^6.0.2"
  }
}
