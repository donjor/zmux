# Copy to .env for local installs.
#
# ZMUX_BIN_DIR="$HOME/.local/bin"
# ZMUX_SKILLS_ROOT="$HOME/path/to/skills"
# ZMUX_SKIP_AGENT_INTEGRATIONS=0
#
# Optional: point dev.sh at a different ignored local extension script.
# ZMUX_DEV_LOCAL="$HOME/path/to/zmux/.dev.local.sh"
