# zmux Keybindings — Source of Truth

> **Generated** from `internal/keys` by `zmux keys gen`. Do not edit by hand —
> change the registry and run `zmux keys gen` (CI fails on drift).

zmux owns three keybinding tables it generates into `tmux.conf` (prefix,
no-prefix, copy-mode) plus a set of inherited tmux defaults it deliberately
keeps. The tables below are rendered from the registry.

## zmux prefix keys (Ctrl+Space, then key)

Custom bindings generated by zmux in `tmux.conf`.

| Action | Key | Description |
|--------|-----|-------------|
| `dashboard` | `Space` | Open zmux dashboard |
| `palette` | `p` | Open command palette |
| `scratch.shell` | `!` | Throwaway $SHELL popup (cwd from active pane) |
| `help` | `?` | Show help |
| `new` | `c` | New tab |
| `tab.next` | `n` | Next tab |
| `tab.prev` | `N` | Previous tab |
| `reorder.left` | `<` | Move tab left |
| `reorder.right` | `>` | Move tab right |
| `kill` | `x` | Close tab (with confirm) |
| `label.tab` | `.` | Set stable tab label (blank clears) |
| `tab.pane` | `J` | Join a tab into this tab as a pane |
| `tab.full` | `F` | Promote focused pane-tab to full tab |
| `session.picker` | `w` | Workspace + session picker |
| `session.goto.N` | `Alt+1-9` | Switch to session N in current workspace |
| `session.prev` | `[` | Previous session in workspace |
| `session.next` | `]` | Next session in workspace |
| `rename` | `,` | Rename session |
| `session.new` | `C` | New session in current workspace |
| `pane.respawn` | `R` | Respawn stopped/dead pane |
| `copy.mode` | `v` | Enter vi copy mode |
| `paste` | `P` | Paste buffer |
| `reload` | `r` | Reload zmux config (zmux apply) |

## No-prefix keys (instant access)

Bound with `-n` in tmux's root table — no prefix needed.

| Action | Key | Description |
|--------|-----|-------------|
| `tab.goto.N` | `Alt+1-9` | Switch to tab N directly |
| `tab.switch` | `` Alt+` `` | Session + tab switcher popup |
| `workspace.switch` | `Alt+w` | Workspace switcher popup |
| `pane.focus.left` | `Alt+Shift+←` | Focus pane left |
| `pane.focus.right` | `Alt+Shift+→` | Focus pane right |
| `pane.focus.up` | `Alt+Shift+↑` | Focus pane up |
| `pane.focus.down` | `Alt+Shift+↓` | Focus pane down |

## Copy mode (vi keys)

| Action | Key | Description |
|--------|-----|-------------|
| `copy.begin` | `v` | Begin selection |
| `copy.rectangle` | `C-v` | Toggle rectangle selection |
| `copy.yank` | `y` | Yank selection to clipboard |
| `copy.cancel` | `Escape` | Cancel copy mode |
| `copy.search.forward` | `/` | Search forward |
| `copy.search.backward` | `?` | Search backward |

## Inherited tmux defaults

tmux keeps these useful pane/session defaults active instead of reinventing
them. They are reached via the prefix and are part of the supported workflow,
but zmux does not generate `bind-key` lines for them.

| Action | Key | Description |
|--------|-----|-------------|
| `detach` | `prefix + d` | Detach from session |
| `pane.focus.direction` | `prefix + Arrow` | Focus pane in direction |
| `pane.next` | `prefix + o` | Focus next pane |
| `pane.last` | `prefix + ;` | Focus previously active pane |
| `pane.ids` | `prefix + q` | Show pane numbers/ids |
| `pane.zoom` | `prefix + z` | Toggle pane zoom |
| `pane.resize.small` | `prefix + Ctrl+Arrow` | Resize pane by one cell |
| `pane.resize.large` | `prefix + Alt+Arrow` | Resize pane by five cells |
| `pane.split.right` | `prefix + %` | Split pane right |
| `pane.split.down` | `prefix + "` | Split pane below |

> `prefix + !` is overridden by zmux for the scratch-shell popup. Bind tmux's
> `break-pane` to a different key in your own conf if you need it.

---

## TUI surfaces

Keys for the interactive surfaces. The **dashboard** keys are defined in the
shared registry (`internal/keys`, Dashboard context) and routed from there by
`internal/tui/dashboard`, so this table is generated. The picker, tab-picker,
and command-palette keys still live in their surface packages
(`internal/tui/...`), scoped to a single Bubble Tea model.

### Picker (workspace picker — outside tmux or popup)

Single flat-list model: top action row + workspaces + nested sessions (when
expanded). Text input is always active, so destructive actions use `ctrl+`
modifiers.

| Action | Key | Notes |
|--------|-----|-------|
| `select` | `enter` | Create / drill in / attach, depending on row |
| `quit` / `cancel` | `esc` (empty) / `ctrl+c` | Esc with text clears query first |
| `kill` | `ctrl+x` | Delete workspace or session under cursor (confirms) |
| `toggle.empty` | `ctrl+h` | Show / hide workspaces with no live sessions |
| `accept.suggestion` | `tab` | Accept ghost autocompletion (workspace name) |
| `navigate.up` / `down` | `up` / `down` | Tree traversal |
| `session.goto.N` | `1-9` | Quick-select session by index (when input empty) |

**Search grammar**: typing fuzzy-matches workspace names. A space separator
filters sessions: `myapp dev` shows sessions matching `dev` within workspaces
matching `myapp`.

### Tab picker (Alt+`)

Text input active → `ctrl+` modifiers.

| Action | Key |
|--------|-----|
| `select` | `enter` |
| `quit` | `esc` / `ctrl+c` |
| `new` | `ctrl+n` |
| `rename` | `ctrl+r` |
| `kill` | `ctrl+x` |
| `reorder.left` / `right` | `<` / `>` |
| `navigate.up` / `down` | `up` / `down` |

### Dashboard tabs

Single-letter keys — the popup captures input directly. Generated from the
registry (Dashboard context); individual tabs add a few surface-local keys (the
Current tab's `h`/`l` window-level nav, the Settings tab's `s` save, the Themes
tab's `e`/`q`) documented in the dashboard's in-app Help.

| Action | Key | Description |
|--------|-----|-------------|
| `select` | `enter` | Focus / switch / edit / expand (depends on row) |
| `create` | `c` | Create at cursor scope (session in workspace, window in session) |
| `create.workspace` | `C` | Create a new workspace |
| `rename` | `r` | Rename selected item |
| `kill` | `x` | Kill / close selected item (confirms) |
| `move` | `m` | Move session / window to another parent |
| `search` | `/` | Filter the list |
| `nav.up` | `up / k` | Move cursor up |
| `nav.down` | `down / j` | Move cursor down |
| `nav.top` | `g` | Jump to top |
| `nav.bottom` | `G` | Jump to bottom |
| `session.goto.N` | `1-9` | Quick-jump to session N (Current tab) |
| `tab.switch.N` | `alt+1-9` | Switch to dashboard tab N |
| `tab.cycle` | `tab / shift+tab` | Cycle to next / previous tab |
| `quit` | `esc / ctrl+c` | Close the dashboard |


### Command palette (prefix+p)

| Action | Key |
|--------|-----|
| filter | type to fuzzy-search all actions |
| `select` | `enter` |
| `quit` | `esc` |
| `navigate.up` / `down` | `j` / `k` |

---

## Design rules

1. **`x` always means kill/close/stop** — `ctrl+x` in pickers, `x` in dashboards, `prefix+x` in tmux.
2. **`d` always means detach** — never kill/delete.
3. **`enter` always means confirm/select.**
4. **`esc` always means cancel/back/quit.**
5. **`c` always means create** — `prefix+c` new tab, `prefix+C` new session, dashboard `c` create at the cursor's scope, dashboard `C` new workspace. Lowercase = narrower scope, uppercase = broader scope.
6. **Pickers use `ctrl+` modifiers** because text input is always active.
7. **Dashboard tabs use single letters** because the popup captures input.
8. **Prefix keys are for tmux-level ops** (tabs, sessions, config).
9. **No-prefix `Alt+` keys are instant** — direct tab/pane actions; `prefix + Alt+1-9` is the matching session jump.
10. **Context resolves same-key conflicts** — `r` = rename (dashboard) vs reload (prefix); `c` = create at scope (dashboard) vs new tab (prefix). Different input contexts mean no real conflict.

## Collision notes for terminal / WM / editor

- **Ghostty:** `Alt+Shift+Arrow`, `Alt+Arrow`, and `Alt+1-9` are unbound locally so they pass through to tmux/zmux. `Ctrl+V` is also unbound so copy-mode rectangle selection works.
- **Hyprland:** uses `Super+Arrow` / `Super+Shift+Arrow` / `Super+Ctrl+Arrow` for window focus/move/resize — these do not consume tmux prefix sequences or no-prefix `Alt+Shift+Arrow`.
- **Pane arrows:** no-prefix `Alt+Shift+Arrow` focuses panes; `prefix + Ctrl+Arrow` / `prefix + Alt+Arrow` resize panes in tmux's prefix table. Keeping resize behind prefix avoids stealing editor `Ctrl+Arrow` and shell `Alt+Arrow` behavior.
- **Neovim/LazyVim:** `Ctrl+h/j/k/l` and `Ctrl+Arrow` are used for window navigation/resize. zmux avoids global no-prefix `Ctrl+h/j/k/l` pane bindings by default; use tmux prefix pane keys instead.
